# Benchmark — 并发 1 / 2 / 4 / 8 吞吐与延迟 (fp8 KV on DGX Spark)

> 标准 vLLM 压测 (`vllm bench serve`)。单机 DGX Spark (GB10 / sm_121)、fp8_e4m3 KV cache、
> `qwen38-flash-fp8-x8` 容器 (vLLM 0.1.dev20073+g8e685d198)。权重 NVFP4, served-model `qwen3.8-flash-next`。

## 测试条件 (每条一致)

| 参数 | 值 |
|---|---|
| 数据集 | `random` |
| input-len / output-len | 1024 / 128 |
| num-prompts | 40 |
| request-rate | `inf` (突发注入,全部 40 条在 t=0 发出) |
| num-warmups | 5 |
| seed | 0 |
| failed | 0 (全部 4 条) |
| KV dtype | `fp8_e4m3` |
| 服务端口 | 宿主 18307 → 容器 8000 |

## 结果

| 并发 | 时长(s) | req/s | **输出 tok/s** | 总 tok/s | TTFT 均值(ms) | TTFT p99(ms) | TPOT 均值(ms) | TPOT p99(ms) |
|---|---|---|---|---|---|---|---|---|
| 1 | 383.02 | 0.104 | **13.37** | 120.31 | 1729.9 | 2211.3 | 61.77 | 63.73 |
| 2 | 227.32 | 0.176 | **22.52** | 202.71 | 3227.7 | 3674.3 | 64.05 | 66.14 |
| 4 | 166.06 | 0.241 | **30.83** | 277.49 | 6732.4 | 7647.5 | 77.70 | 83.90 |
| 8 | 120.44 | 0.332 | **42.51** | 382.60 | 10814.1 | 13070.2 | 104.45 | 147.90 |

原始 JSON：`concurrency{1,2,4,8}.json` (`/bench`, 容器内)。

## 读图要点

1. **聚合吞吐随并发亚线性提升。** 总 tok/s 从 1→8 并发只涨 **3.18×** (120.31 → 382.60),
   而非理想 8×;每翻倍约 +1.4×。说明 GPU 算力在 ~8 并发时已被共享得较充分,没有干净的线性区。
2. **单请求首字延迟 (TTFT) 爆炸式恶化。** 1729.9 → 10814.1 ms (+6.3×),约每次翻倍就翻一番。
   并发 8 时,一次完整请求要等 ~10.8s 才出首字。这是 `request-rate=inf` 突发下,请求在服务端
   排队的累计 —— 峰值 `max_concurrent_requests` 达 16 (≈2× max-concurrency)。
3. **解码速率 (TPOT) 持续劣化。** 61.77 → 104.45 ms (+69%),集中在 4→8 (+34%)。
   每条流的每秒生成速率从 ~16 tok/s 掉到 ~9.6 tok/s —— 越并发,单流越慢。
4. **本质是「吞吐 vs 延迟」的取舍。** fp8-KV 能拉高聚合吞吐,代价是单请求延迟显著变贵。
   到 8 并发 (容器 `--max-num-seqs 8` 上限) 仍未见到吞吐饱和,但每请求成本已明显上升。

## 备注 / 偏差

- 本测为**小上下文中等负载** (1024 in / 128 out),只反映 decode 聚合,不触及 262k 长上下文
  / 稀疏注意力检索路径的极限。
- 突发注入 (request-rate=inf) 使 TTFT 主要为排队延迟;稳态低请求率下 TTFT 会显著低于此。
- fp8 未标定 (scale=1.0),与 bf16 的正确性差异见 `README.md` 的 Results 一节。
