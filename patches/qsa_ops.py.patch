--- a/vllm/models/qwen3_8_flash_next/nvidia/ops/qsa.py
+++ b/vllm/models/qwen3_8_flash_next/nvidia/ops/qsa.py
@@ -215,6 +215,8 @@
     num_rows,
     num_cache_blocks,
     num_requests,
+    k_scale,
+    v_scale,
     TOPK: tl.constexpr,
     PAGE_SIZE: tl.constexpr,
     PAGE_TABLE_WIDTH: tl.constexpr,
@@ -297,6 +299,10 @@
             mask=valid[:, None],
             other=0.0,
         )
+        # Dequantize FP8 K/V to BF16 (no-op for a BF16 cache with a scale of
+        # 1.0) so the dots below run on the same dtypes as the BF16 path.
+        keys = (keys.to(tl.float32) * k_scale).to(tl.bfloat16)
+        values = (values.to(tl.float32) * v_scale).to(tl.bfloat16)
         scores = tl.dot(query, keys)
         # Scaling scores avoids re-quantizing a scaled query to BF16.
         scores *= softmax_scale_log2
@@ -817,8 +823,10 @@
     block_table: torch.Tensor,
     token_to_req: torch.Tensor,
     out: torch.Tensor | None = None,
+    k_scale: float = 1.0,
+    v_scale: float = 1.0,
 ) -> torch.Tensor:
-    """Run sparse GQA directly over paged BF16 K/V caches."""
+    """Run sparse GQA directly over paged BF16/FP8 K/V caches."""
 
     if not q.is_cuda or not HAS_TRITON:
         raise RuntimeError("paged QSA sparse attention requires CUDA and Triton")
@@ -836,7 +844,13 @@
         raise ValueError("QSA sparse attention requires valid grouped-query heads")
     head_dim = q.shape[2]
     assert head_dim >= 16 and (head_dim & (head_dim - 1)) == 0
-    assert q.dtype == k_cache.dtype == v_cache.dtype == torch.bfloat16
+    assert q.dtype == torch.bfloat16
+    assert k_cache.dtype == v_cache.dtype
+    assert k_cache.dtype in (
+        torch.bfloat16,
+        torch.float8_e4m3fn,
+        torch.float8_e5m2,
+    )
     assert logical_indices.dtype == block_table.dtype == torch.int32
     assert token_to_req.dtype == torch.int32
     assert q.device == k_cache.device == v_cache.device
@@ -919,6 +933,8 @@
         q.shape[0],
         k_cache.shape[0],
         block_table.shape[0],
+        k_scale,
+        v_scale,
         TOPK=logical_indices.shape[1],
         PAGE_SIZE=k_cache.shape[1],
         PAGE_TABLE_WIDTH=block_table.shape[1],
@@ -930,7 +946,11 @@
         BLOCK_M=block_m,
         BLOCK_N=block_n,
         num_warps=partial_warps,
-        num_stages=2,
+        # The original kernel was tuned on GB300 (227 KB smem/SM). On DGX
+        # Spark's sm_121 the 100 KB smem cap is exceeded at num_stages=2 for
+        # FP8 (106 KB) and BF16 (136 KB). num_stages=1 fits both; it only
+        # changes the load pipeline depth, not the numerics.
+        num_stages=1,
     )
     if num_splits == 1:
         return out
